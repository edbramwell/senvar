{
  "name": "@senvar/example-aws-sst-lambda",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "sst dev",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "postinstall": "node scripts/postinstall.js"
  },
  "dependencies": {
    "@senvar/secrets": "file:../../..",
    "sst": "^3.0.69"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}

